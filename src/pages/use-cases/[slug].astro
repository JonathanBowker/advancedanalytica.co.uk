---
export const prerender = false;

const { slug } = Astro.params as { slug: string };
const target = `/case-studies/${encodeURIComponent(slug)}${Astro.url.search}`;
return Astro.redirect(target, 301);
---
