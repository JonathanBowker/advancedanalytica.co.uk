---
export const prerender = false;

const { series } = Astro.params as { series: string };
const target = `/opinions/series/${encodeURIComponent(series)}${Astro.url.search}`;
return Astro.redirect(target, 301);
---
