---
export const prerender = false;

const { tag } = Astro.params as { tag: string };
const target = `/opinions/tag/${encodeURIComponent(tag)}${Astro.url.search}`;
return Astro.redirect(target, 301);
---
