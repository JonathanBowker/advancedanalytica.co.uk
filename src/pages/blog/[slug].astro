---
export const prerender = false;

const { slug } = Astro.params as { slug: string };
const target = `/opinions/${encodeURIComponent(slug)}${Astro.url.search}`;
return Astro.redirect(target, 301);
---
